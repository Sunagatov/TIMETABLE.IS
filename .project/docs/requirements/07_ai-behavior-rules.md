# AI Behavior Rules

## Principle
AI should improve clarity and structure without silently changing the user's intended meaning.

## Current state
The runnable starter uses lightweight placeholder normalization/inference logic to keep the system startable.

## Future target
Real AI integration should:
- remain semantically faithful
- avoid silently replacing intended meaning
- stay within the approved category path rules
